eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: product
server:
  port: 8082
#----------------------------- swagger应用 ----------------------------------
knife4j:
  # 开启增强配置
  enable: true
  # 开启生产环境屏蔽
  production: false
